# Backend Agent Rules

- **Refer to the root `docs/` folder** for project-wide rules, architecture, and API specs.
- This backend is written in **Go + Gin + GORM**.
- Database is **PostgreSQL (Supabase)**.
- Local development: `go run ./cmd/api`.
- DO NOT duplicate shared documentation in this directory.
